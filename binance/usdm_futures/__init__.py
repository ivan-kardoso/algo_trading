"""Pacote raiz do robô USDM Futures (Binance).

Responsabilidade:
    Marcar `usdm_futures` como um pacote Python importável e expor a versão
    do projeto. Não contém lógica de negócio — é apenas o ponto de identidade
    do pacote, sobre o qual as próximas camadas (config, infra, domínio,
    serviços, máquina de estados e aplicação) serão construídas.
"""

__version__ = "0.1.0"
